<div>
  <p class=comment_<%= comment.id %>><strong>Comment: </strong><%= comment.content %>
  <small>Posted
    <%= distance_of_time_in_words(comment.job.created_at, comment.created_at) %>
    ago</small></p>
</div>
